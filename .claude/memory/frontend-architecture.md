# AI Smart Diet Lens - 前端架构文档

> **项目**: AI Smart Diet Lens
> **技术栈**: UniApp + Vue 3 + TypeScript + UnoCSS
> **最后更新**: 2026-02-06
> **版本**: 2.1.0

---

## 目录

1. [项目概述](#项目概述)
2. [项目结构](#项目结构)
3. [页面清单](#页面清单)
4. [导航架构](#导航架构)
5. [组件库](#组件库)
6. [样式系统](#样式系统)
7. [后端 API 需求](#后端-api-需求)
8. [数据模型](#数据模型)
9. [状态管理](#状态管理)
10. [本地存储](#本地存储)
11. [开发指南](#开发指南)
12. [待完善功能](#待完善功能)

---

## 项目概述

AI Smart Diet Lens 是一款智能营养追踪与膳食规划应用，通过 AI 拍照识别食物，帮助用户管理健康饮食。

### 核心功能

- **AI 食物识别**: 拍照自动识别食物并计算营养成分
- **每日营养追踪**: 记录每餐摄入，可视化展示热量和营养素
- **食材百科**: 浏览食材营养信息，获取 AI 健康推荐
- **社区动态**: 分享饮食，与好友互动
- **AI 营养师对话**: 通过对话获取个性化餐单建议
- **游戏化成就**: 解锁健康成就，激励持续使用

### 目标平台

- 微信小程序 (主要)
- H5 网页版
- 支付宝小程序 (规划中)
- App (规划中)

---

## 项目结构

```
frontend/packages/ui/src/
├── App.vue                      # 应用入口，全局样式
├── main.ts                      # 应用启动
├── components/
│   └── BottomNav.vue            # 自定义底部导航栏 (5个Tab + FAB)
├── pages/
│   ├── index/                   # Tab1: 首页仪表盘
│   ├── diary/                   # Tab2: 饮食日记
│   ├── scan/                    # Tab3: 拍照识别 (FAB)
│   ├── discover/                # Tab4: 发现 (百科 + 社区)
│   ├── profile/                 # Tab5: 个人中心
│   ├── wiki/                    # 食材百科 (已整合到 discover)
│   ├── messages/                # 消息中心
│   ├── settings/                # 设置页面
│   ├── analysis/                # 数据分析
│   ├── shopping-list/           # 购物清单
│   ├── food-result/             # 营养确认
│   ├── food-detail/             # 食材详情
│   ├── achievements/            # 成就系统
│   ├── cooking-assistant/       # AI烹饪助手
│   ├── recipe-detail/           # 食谱详情
│   ├── scan-fail/               # 识别失败
│   ├── splash/                  # 启动页
│   ├── ai-chat/                 # AI营养师对话 (NEW)
│   ├── favorites/               # 我的收藏
│   ├── my-posts/                # 我的发布
│   ├── my-reviews/              # 我的评价
│   └── onboarding/              # 引导流程
│       ├── index                # 欢迎页
│       ├── login                # 登录
│       ├── register             # 注册
│       ├── body-metrics         # 身体数据
│       └── goals                # 目标选择
├── pages.config.ts              # 页面配置 (自动生成 pages.json)
└── manifest.config.ts           # 应用配置 (自动生成 manifest.json)
```

---

## 页面清单

### Tab 页面 (5个)

| 页面 | 路径 | 功能 | 导航标识 |
|:-----|:-----|:-----|:---------|
| **首页** | `/pages/index/index` | 热量环形图、健康指标、AI助手入口 | `home` |
| **日记** | `/pages/diary/index` | 每日营养记录、周视图、餐食详情 | `menu_book` |
| **识别** | `/pages/scan/index` | 相机拍照、相册选择、AI识别 | `photo_camera` (FAB) |
| **发现** | `/pages/discover/index` | 百科搜索 + 社区动态 (双标签) | `explore` |
| **我的** | `/pages/profile/index` | 个人信息、成就、设置入口 | `person` |

### 功能页面 (17个)

| 页面 | 路径 | 功能 | 导航来源 |
|:-----|:-----|:-----|:---------|
| **AI营养师对话** | `/pages/ai-chat/index` | 对话式获取个性化餐单 | 首页「对话定制」 |
| **消息中心** | `/pages/messages/index` | 系统通知、私信、互动消息 | 个人中心 |
| **设置** | `/pages/settings/index` | 个人信息、健康同步、通知设置 | 个人中心 |
| **数据分析** | `/pages/analysis/index` | 营养趋势、周/月切换、AI建议 | 日记页 |
| **购物清单** | `/pages/shopping-list/index` | AI生成清单、分类浏览、一键下单 | 首页/个人中心 |
| **营养确认** | `/pages/food-result/index` | 识别后确认营养、调整份量 | 拍照识别 |
| **食材详情** | `/pages/food-detail/index` | 单个食材的详细营养信息 | 发现/百科 |
| **成就系统** | `/pages/achievements/index` | 勋章展示、成就进度 | 个人中心 |
| **AI烹饪助手** | `/pages/cooking-assistant/index` | 智能食谱推荐、烹饪指导 | 首页「直接生成」 |
| **食谱详情** | `/pages/recipe-detail/index` | 食谱步骤、食材清单 | 烹饪助手 |
| **识别失败** | `/pages/scan-fail/index` | 重试、手动输入 | 拍照识别 |
| **我的收藏** | `/pages/favorites/index` | 收藏的食谱/食材 | 个人中心 |
| **我的发布** | `/pages/my-posts/index` | 用户发布的社区动态 | 个人中心 |
| **我的评价** | `/pages/my-reviews/index` | 用户对食谱的评价 | 个人中心 |

### 引导流程 (6个)

| 页面 | 路径 | 功能 |
|:-----|:-----|:-----|
| **启动页** | `/pages/splash/index` | Logo展示、初始化检查、自动跳转 |
| **欢迎页** | `/pages/onboarding/index` | 功能介绍、开始使用 |
| **登录** | `/pages/onboarding/login` | 手机号/邮箱登录 |
| **注册** | `/pages/onboarding/register` | 新用户注册 |
| **身体数据** | `/pages/onboarding/body-metrics` | 身高、体重、年龄、性别输入 |
| **目标选择** | `/pages/onboarding/goals` | 减脂/增肌/健康目标选择 |

**页面总数**: 22 个页面

---

## 导航架构

### 底部导航 (BottomNav)

```
┌─────────────────────────────────────────────────────────────┐
│  Tab1    Tab2    [FAB]    Tab4    Tab5                       │
│  首页    日记    相机     发现    我的                        │
└─────────────────────────────────────────────────────────────┘
```

**导航项配置**:
```typescript
const navItems: NavItem[] = [
  { path: "/pages/index/index", icon: "home", label: "首页" },
  { path: "/pages/diary/index", icon: "menu_book", label: "日记" },
  { path: "/pages/scan/index", icon: "photo_camera", label: "识别", isFab: true },
  { path: "/pages/discover/index", icon: "explore", label: "发现" },
  { path: "/pages/profile/index", icon: "person", label: "我的" },
];
```

### Tab4 发现页架构 (简化版)

```
发现页面 (discover/index)
├── 顶部两个主标签:
│   ├── 🔍 百科 (wiki)
│   │   ├── 搜索栏
│   │   ├── 分类筛选 (超级食物、优质蛋白、低GI、维生素)
│   │   ├── 当季 AI 推荐
│   │   └── 食材网格
│   │
│   └── 👥 社区 (community)
│       └── 帖子信息流 (点赞、评论、分享)
```

### 首页 AI 助手卡片

```
首页 (index/index)
└── AI 智能烹饪助手卡片
    ├── 「直接生成」按钮 → /pages/cooking-assistant/index
    └── 「对话定制」按钮 → /pages/ai-chat/index
```

### 个人中心导航

```
个人中心 (profile/index)
├── 头像/昵称/简介
├── 快捷入口
│   ├── 我的成就 → /pages/achievements/index
│   ├── 我的收藏 → /pages/favorites/index
│   └── 购物清单 → /pages/shopping-list/index
├── 数据分析入口 → /pages/analysis/index
├── 我的发布入口 → /pages/my-posts/index
├── 我的评价入口 → /pages/my-reviews/index
├── 消息中心入口 → /pages/messages/index
└── 设置入口 → /pages/settings/index
```

---

## 组件库

### 核心组件

| 组件 | 路径 | 功能说明 |
|:-----|:-----|:---------|
| **BottomNav** | `/components/BottomNav.vue` | 自定义底部导航，5个Tab + FAB相机按钮 |

### BottomNav 组件详情

**特性**:
- 5个Tab项 + 1个FAB (中心浮动按钮)
- Material Symbols Outlined 图标
- 激活状态图标填充效果 (`FILL` font-variation-settings)
- 安全区域适配 (`env(safe-area-inset-bottom)`)
- 毛玻璃背景 (`backdrop-blur-xl`)

**导航逻辑**:
```typescript
const navigateToTab = (path: string) => {
  uni.switchTab({
    url: path,
    fail: (err) => {
      // 如果 switchTab 失败（可能不是 Tab 页面），尝试 navigateTo
      uni.navigateTo({ url: path });
    },
  });
};
```

---

## 样式系统

### 设计规范

#### 颜色系统

```css
/* 主色调 Sage Green */
--primary: #649678;           /* 主色 */
--primary-light: #38e07b;     /* 亮色 (激活状态) */
--primary-dark: #4a755c;      /* 暗色 */

/* 中性色 */
--bg-base: #F5F7F8;           /* 页面背景 */
--bg-white: #FFFFFF;          /* 卡片背景 */
--text-primary: #1C1C1E;      /* 主标题 */
--text-secondary: #5a847b;    /* 次要文字 */
--border-color: #e5e7eb;     /* 边框 */
```

#### 图标系统

**Material Symbols Outlined** (Google Fonts)

```css
/* 基础样式 */
.material-symbols-outlined {
  font-family: "Material Symbols Outlined";
  font-weight: normal;
  font-style: normal;
  font-size: 24px;
}

/* 填充效果 (激活状态) */
.filled {
  font-variation-settings:
    "FILL" 1,
    "wght" 400,
    "GRAD" 0,
    "opsz" 48;
}
```

**常用图标映射**:
| 功能 | 图标名称 | 说明 |
|:-----|:---------|:-----|
| 首页 | `home` | 主页 |
| 日记 | `menu_book` | 饮食记录 |
| 相机 | `photo_camera` | 拍照识别 |
| 发现 | `explore` | 探索 |
| 我的 | `person` | 个人中心 |
| 搜索 | `search` | 搜索栏 |
| 收藏 | `favorite` | 收藏/喜欢 |
| 设置 | `settings` | 设置 |
| 返回 | `arrow_back_ios_new` | 返回上一页 |
| 对话 | `chat_bubble` | 对话/聊天 |

---

## 后端 API 需求

### AI 对话模块 (`/api/ai/chat`)

| 端点 | 方法 | 功能 | 请求参数 | 响应数据 |
|:-----|:-----|:-----|:---------|:---------|
| `/message` | POST | 发送对话消息 | `message`, `context` | `ChatResponse` |
| `/recipe` | POST | 根据对话生成食谱 | `preferences` | `Recipe` |

```typescript
interface ChatResponse {
  reply: string
  recipeCard?: {
    name: string
    image: string
    calories: number
    time: string
    difficulty: string
    description: string
  }
}
```

### 其他模块 (同 v2.0)

- 用户认证 (`/api/auth`)
- 用户信息 (`/api/user`)
- AI 识别 (`/api/ai/analyze`)
- 饮食日记 (`/api/diary`)
- 数据分析 (`/api/analytics`)
- 食材百科 (`/api/food`)
- 购物清单 (`/api/shopping`)
- 社区 (`/api/community`)
- 收藏 (`/api/favorites`)
- 评价 (`/api/reviews`)
- 消息 (`/api/messages`)
- 成就 (`/api/achievements`)

---

## 数据模型

### ChatMessage (对话消息)

```typescript
interface ChatMessage {
  id: string
  isUser: boolean
  content: string
  timestamp: string
  recipeCard?: {
    name: string
    image: string
    calories: number
    time: string
    difficulty: string
    description: string
  }
}
```

### 其他数据模型 (同 v2.0)

- User, UserProfile, Food, DiaryEntry, CommunityPost, Recipe, Achievement, ShoppingItem

---

## 开发指南

### 导航跳转

```typescript
// Tab 页面切换 (使用 switchTab)
uni.switchTab({
  url: '/pages/index/index'
})

// 普通页面跳转 (使用 navigateTo)
uni.navigateTo({
  url: '/pages/ai-chat/index'
})

// 返回上一页
uni.navigateBack({
  delta: 1
})
```

---

## 待完善功能

### 需要后端支持

1. **用户认证**: 登录/注册/Token刷新
2. **AI 对话**: 营养师对话 API
3. **AI 识别**: Gemini API 集成
4. **数据存储**: 营养记录、历史数据
5. **社区功能**: 发布、点赞、评论、关注
6. **消息推送**: 系统通知、私信
7. **购物集成**: 盒马、美团 API
8. **健康数据**: Apple HealthKit 同步

### 需要前端优化

1. **状态管理**: 实现 Pinia stores
2. **请求封装**: 统一 API 调用和错误处理
3. **加载状态**: 全局 Loading 组件
4. **图片上传**: 压缩、Base64 转换
5. **离线支持**: 本地缓存策略
6. **性能优化**: 图片懒加载、列表虚拟化
7. **测试**: 单元测试、E2E 测试

---

## 版本历史

| 版本 | 日期 | 说明 |
|:-----|:-----|:-----|
| 1.0.0 | 2026-02-06 | 初始版本，完成18个UI页面 |
| 2.0.0 | 2026-02-06 | 更新发现页为双模式架构，新增收藏/发布/评价页面 |
| 2.1.0 | 2026-02-06 | 简化发现页架构（移除冗余子标签），新增AI营养师对话页面，首页支持两种定制方式 |

---

**文档维护者**: Architect
**最后更新**: 2026-02-06
